[project]
name = "basin-ingest"
version = "0.1.0"
description = "Basin ingestion — Dagster assets and source adapters"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["basin_ingest*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
